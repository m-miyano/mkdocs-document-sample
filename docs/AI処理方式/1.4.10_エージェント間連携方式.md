# 1.4.10 エージェント間連携方式

---

## 目次

- [1. 概要](#1-概要)
- [2. 連携アーキテクチャ全体像](#2-連携アーキテクチャ全体像)
- [3. エージェント種別と役割](#3-エージェント種別と役割)
- [4. 連携パターン](#4-連携パターン)
  - [4.1 オーケストレーター型](#41-オーケストレーター型)
  - [4.2 コレオグラフィー型（イベント駆動）](#42-コレオグラフィー型イベント駆動)
  - [4.3 ピアツーピア型](#43-ピアツーピア型)
- [5. メッセージプロトコル](#5-メッセージプロトコル)
- [6. コンテキスト共有方式](#6-コンテキスト共有方式)
- [7. エラーハンドリング・リトライ方式](#7-エラーハンドリングリトライ方式)
- [8. 制御フロー詳細シーケンス](#8-制御フロー詳細シーケンス)
- [9. 状態管理](#9-状態管理)
- [10. 非機能要件対応](#10-非機能要件対応)

---

## 1. 概要

本章では、マルチエージェントシステムにおけるエージェント間の連携方式について定義する。
各エージェントは独立したLLMコンテキストと専門的な役割を持ち、協調して複雑なタスクを達成する。

| 項目 | 内容 |
|------|------|
| **対象システム** | AIアシスタント基盤（マルチエージェント構成） |
| **連携方式** | オーケストレーター型 ＋ イベント駆動型の複合構成 |
| **通信プロトコル** | 内部バス（同期）/ メッセージキュー（非同期） |
| **コンテキスト共有** | 共有メモリ（Redis） + エージェント固有メモリ |
| **採用フレームワーク** | LangGraph / AutoGen（用途に応じて使い分け） |

---

## 2. 連携アーキテクチャ全体像

```mermaid
graph TB
    subgraph USER["👤 ユーザーインターフェース層"]
        UI[Webフロントエンド]
        API[API Gateway]
    end

    subgraph ORCH["🎼 オーケストレーター層"]
        OA[Orchestrator Agent<br/>タスク分解・ルーティング]
    end

    subgraph AGENTS["🤖 専門エージェント層"]
        direction LR
        SA[Search Agent<br/>情報検索]
        CA[Coder Agent<br/>コード生成・実行]
        DA[Data Agent<br/>データ分析]
        RA[Review Agent<br/>品質レビュー]
        WA[Writer Agent<br/>文書生成]
    end

    subgraph INFRA["🗄️ 共有インフラ層"]
        direction LR
        SM[Shared Memory<br/>Redis]
        VDB[Vector DB<br/>Qdrant]
        MQ[Message Queue<br/>RabbitMQ]
        TS[Tool Server<br/>MCP]
    end

    UI --> API --> OA
    OA -->|タスク委譲| SA
    OA -->|タスク委譲| CA
    OA -->|タスク委譲| DA
    OA -->|レビュー依頼| RA
    OA -->|文書化依頼| WA

    SA <-->|コンテキスト読書き| SM
    CA <-->|コンテキスト読書き| SM
    DA <-->|コンテキスト読書き| SM
    RA <-->|コンテキスト読書き| SM
    WA <-->|コンテキスト読書き| SM

    SA --> VDB
    DA --> VDB
    SA --> TS
    CA --> TS

    OA -->|非同期タスク| MQ
    MQ -->|ワーカー起動| CA
    MQ -->|ワーカー起動| DA

    style USER fill:#e3f2fd,stroke:#1565c0
    style ORCH fill:#fce4ec,stroke:#880e4f
    style AGENTS fill:#e8f5e9,stroke:#1b5e20
    style INFRA fill:#fff8e1,stroke:#e65100
```

---

## 3. エージェント種別と役割

| エージェント名 | 略称 | 主な役割 | 使用LLM | 連携先 |
|---|---|---|---|---|
| Orchestrator Agent | OA | タスク分解・委譲・結果集約 | GPT-4o | 全エージェント |
| Search Agent | SA | Web/DB/社内文書検索 | GPT-4o-mini | DA, WA |
| Coder Agent | CA | コード生成・実行・デバッグ | Claude 3.5 Sonnet | RA, DA |
| Data Agent | DA | データ処理・統計・可視化 | GPT-4o | WA, RA |
| Review Agent | RA | 成果物の品質レビュー・修正提案 | GPT-4o | OA（フィードバック） |
| Writer Agent | WA | レポート・ドキュメント生成 | Claude 3.5 Sonnet | OA（最終成果物） |

### エージェント責務の詳細

- **Orchestrator Agent**
  - ユーザーリクエストを受け取り、サブタスクへ分解（Task Decomposition）
  - 依存関係を解析し、実行順序を決定（DAG構築）
  - 各エージェントの結果を収集・統合し、最終回答を生成

- **Search Agent**
  - RAGパイプラインを通じた関連ドキュメント取得
  - Web検索ツール（Bing API / Tavily）の呼び出し
  - 検索結果のスコアリングと要約

- **Coder Agent**
  - コードインタープリター（Python Sandbox）による安全な実行
  - テスト生成・実行によるコード検証
  - 実行結果をStructured Outputとして返却

---

## 4. 連携パターン

### 4.1 オーケストレーター型

中央集権的に OA がすべての制御を担うパターン。複雑なワークフローに適する。

```mermaid
sequenceDiagram
    actor User
    participant OA as 🎼 Orchestrator
    participant SA as 🔍 Search Agent
    participant CA as 💻 Coder Agent
    participant RA as 🔎 Review Agent

    User->>+OA: 「Pythonでデータ分析スクリプトを書いて」
    OA->>OA: タスク分解<br/>① 要件整理 → ② コード生成 → ③ レビュー

    OA->>+SA: 関連ライブラリ・ベストプラクティスを検索
    SA-->>-OA: 検索結果（pandas, matplotlib, scikit-learn等）

    OA->>+CA: [検索結果を添付] コード生成指示
    CA->>CA: コード生成 & Sandbox実行
    CA-->>-OA: 生成コード + 実行結果

    OA->>+RA: [コード] レビュー依頼
    RA-->>-OA: レビュー結果（LGTM / 修正コメント）

    alt レビューOK
        OA-->>User: 最終コード + 説明
    else 修正が必要
        OA->>CA: 修正指示（再帰呼び出し）
    end
```

---

### 4.2 コレオグラフィー型（イベント駆動）

エージェントがイベントを購読し、自律的に連鎖するパターン。疎結合・スケーラブル。

```mermaid
graph LR
    subgraph MQ["📨 Message Queue (RabbitMQ)"]
        direction TB
        T1([task.search.requested])
        T2([task.code.requested])
        T3([task.review.requested])
        T4([task.complete])
    end

    OA[🎼 Orchestrator] -->|publish| T1
    T1 -->|subscribe| SA[🔍 Search Agent]
    SA -->|publish| T2
    T2 -->|subscribe| CA[💻 Coder Agent]
    CA -->|publish| T3
    T3 -->|subscribe| RA[🔎 Review Agent]
    RA -->|publish| T4
    T4 -->|subscribe| OA

    style MQ fill:#fff3e0,stroke:#e65100
```

#### イベントスキーマ定義

```json
{
  "event_id": "uuid-v4",
  "event_type": "task.code.requested",
  "source_agent": "orchestrator",
  "target_agent": "coder",
  "timestamp": "2026-07-03T10:00:00Z",
  "correlation_id": "session-uuid",
  "payload": {
    "task_id": "task-001",
    "instruction": "データ分析スクリプトを生成してください",
    "context_keys": ["search_result_001", "user_requirements"],
    "priority": "normal",
    "timeout_sec": 60
  }
}
```

---

### 4.3 ピアツーピア型

特定のエージェントペアが直接通信するパターン。低レイテンシが求められる場面で使用。

```mermaid
graph TD
    CA[💻 Coder Agent] <-->|直接RPC| RA[🔎 Review Agent]
    CA -->|修正依頼| CA
    RA -->|承認| Done([✅ 完了])

    style Done fill:#c8e6c9,stroke:#388e3c
```

---

## 5. メッセージプロトコル

エージェント間のメッセージは統一フォーマットで送受信する。

### メッセージ構造

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `message_id` | UUID | ✅ | メッセージ一意識別子 |
| `correlation_id` | UUID | ✅ | セッション・タスクの追跡用ID |
| `sender` | string | ✅ | 送信エージェントID |
| `receiver` | string | ✅ | 受信エージェントID（`broadcast`も可） |
| `message_type` | enum | ✅ | `request` / `response` / `event` / `error` |
| `priority` | enum | ✅ | `high` / `normal` / `low` |
| `payload` | object | ✅ | メッセージ本文（タスク依存） |
| `context_refs` | array | - | 参照する共有メモリのキー一覧 |
| `created_at` | ISO8601 | ✅ | 送信日時 |
| `expires_at` | ISO8601 | - | メッセージ有効期限 |

### メッセージタイプ詳細

| タイプ | 通信方式 | 用途 | 応答要否 |
|---|---|---|---|
| `request` | 同期 / 非同期 | タスク委譲・ツール呼び出し要求 | ✅ 必要 |
| `response` | 同期 / 非同期 | requestへの応答・成果物返却 | ❌ 不要 |
| `event` | 非同期（Pub/Sub） | 状態変化の通知 | ❌ 不要 |
| `error` | 同期 / 非同期 | エラー・例外の通知 | ❌ 不要 |

---

## 6. コンテキスト共有方式

エージェントは独立したLLMコンテキストを持つため、情報共有は外部ストアを経由する。

```mermaid
graph LR
    subgraph AGENTS["エージェント群"]
        OA[🎼 OA]
        SA[🔍 SA]
        CA[💻 CA]
    end

    subgraph SHARED["共有ストア"]
        direction TB
        SM["🔴 Redis<br/>(短期・揮発性)<br/>TTL: 1時間"]
        DB["🟢 PostgreSQL<br/>(長期・永続)<br/>セッション跨ぎ保存"]
        VDB["🔵 Qdrant<br/>(ベクトル検索)<br/>RAGコンテキスト"]
    end

    OA <-->|read/write| SM
    SA <-->|read/write| SM
    CA <-->|read/write| SM
    OA -->|永続化| DB
    SA -->|永続化| DB
    SM -->|フラッシュ| DB
    SA <-->|検索| VDB
```

### コンテキストキー命名規則

```
{session_id}:{task_id}:{agent_id}:{data_type}

例）
sess-abc123:task-001:search-agent:search_results
sess-abc123:task-001:coder-agent:generated_code
sess-abc123:task-001:orchestrator:task_plan
```

---

## 7. エラーハンドリング・リトライ方式

```mermaid
flowchart TD
    START([エージェント呼び出し]) --> EXEC[タスク実行]
    EXEC --> CHECK{成功？}
    CHECK -->|Yes| SUCCESS([✅ 結果返却])
    CHECK -->|No| CLASSIFY{エラー種別}

    CLASSIFY -->|タイムアウト| RETRY{リトライ回数\n< MAX？}
    CLASSIFY -->|モデルエラー| FALLBACK[フォールバック\nモデルに切替]
    CLASSIFY -->|ツールエラー| TOOLFIX[ツール再試行\n or 代替ツール]
    CLASSIFY -->|致命的エラー| ESCALATE[🆙 Orchestratorへ\nエスカレーション]

    RETRY -->|Yes| WAIT[指数バックオフ待機\n1s → 2s → 4s] --> EXEC
    RETRY -->|No| ESCALATE

    FALLBACK --> EXEC
    TOOLFIX --> EXEC
    ESCALATE --> DEGRADED[縮退運転 or\nユーザーへ通知]

    style SUCCESS fill:#c8e6c9,stroke:#388e3c
    style ESCALATE fill:#ffcdd2,stroke:#c62828
    style DEGRADED fill:#fff9c4,stroke:#f57f17
```

### リトライポリシー

| エラー種別 | リトライ | 最大回数 | バックオフ | フォールバック |
|---|---|---|---|---|
| ネットワークタイムアウト | ✅ | 3回 | 指数（1s, 2s, 4s） | - |
| LLMレート制限（429） | ✅ | 5回 | 指数 + Jitter | 別エンドポイント |
| LLMモデルエラー（5xx） | ✅ | 2回 | 固定（2s） | フォールバックモデル |
| 入力バリデーションエラー | ❌ | - | - | Orchestratorへ通知 |
| コンテキスト長超過 | ❌ | - | - | チャンク分割して再実行 |

---

## 8. 制御フロー詳細シーケンス

複合タスク（「競合他社の最新動向を調査してレポートを作成せよ」）の完全な実行フロー。

```mermaid
sequenceDiagram
    actor User
    participant OA as 🎼 Orchestrator
    participant SM as 🔴 Redis
    participant SA as 🔍 Search Agent
    participant DA as 📊 Data Agent
    participant WA as ✍️ Writer Agent
    participant RA as 🔎 Review Agent

    User->>+OA: 「競合他社の最新動向を調査してレポートを作成して」

    rect rgb(232, 245, 233)
        Note over OA: Phase 1: タスク分解
        OA->>OA: タスクをDAGに変換
        OA->>SM: タスクプランを保存<br/>key: sess-xxx:task_plan
    end

    rect rgb(227, 242, 253)
        Note over SA: Phase 2: 情報収集（並列）
        par 並列実行
            OA->>+SA: Web検索依頼（競合A）
            SA-->>-OA: 検索結果A
        and
            OA->>+SA: Web検索依頼（競合B）
            SA-->>-OA: 検索結果B
        end
        OA->>SM: 検索結果を保存
    end

    rect rgb(255, 243, 224)
        Note over DA: Phase 3: データ分析
        OA->>+DA: [検索結果参照] 比較分析依頼
        DA->>SM: 検索結果を読み込み
        DA->>DA: 比較表・グラフ生成
        DA->>SM: 分析結果を保存
        DA-->>-OA: 分析完了通知
    end

    rect rgb(252, 228, 236)
        Note over WA,RA: Phase 4: 文書生成 & レビュー
        OA->>+WA: [全結果参照] レポート作成依頼
        WA->>SM: コンテキスト一括読み込み
        WA->>WA: Markdownレポート生成
        WA-->>-OA: 草稿レポート

        OA->>+RA: レビュー依頼
        RA-->>-OA: レビュー結果（修正2点）

        OA->>+WA: 修正指示
        WA-->>-OA: 最終レポート
    end

    OA-->>-User: 最終レポートを返却
```

---

## 9. 状態管理

各タスクの実行状態はステートマシンで管理する。

```mermaid
stateDiagram-v2
    [*] --> PENDING : タスク受付

    PENDING --> RUNNING : エージェント割り当て完了
    PENDING --> CANCELLED : タイムアウト / キャンセル要求

    RUNNING --> WAITING_DEPENDENCY : 依存タスク未完了
    WAITING_DEPENDENCY --> RUNNING : 依存タスク完了

    RUNNING --> REVIEWING : レビュー待ち
    REVIEWING --> RUNNING : 修正依頼（フィードバックループ）
    REVIEWING --> COMPLETED : レビュー承認

    RUNNING --> FAILED : エラー（リトライ上限超過）
    FAILED --> RUNNING : 手動リトライ / 縮退実行

    COMPLETED --> [*]
    CANCELLED --> [*]
    FAILED --> [*]

    note right of RUNNING
        最大実行時間: 300秒
        ウォッチドッグタイマーで監視
    end note

    note right of REVIEWING
        最大レビューループ: 3回
        超過時はOAが強制終了
    end note
```

---

## 10. 非機能要件対応

### パフォーマンス要件

| 指標 | 目標値 | 計測方法 |
|---|---|---|
| エージェント間メッセージ遅延 | < 100ms（同期） | APM（Datadog） |
| 非同期タスクキュー遅延 | < 500ms（通常負荷時） | RabbitMQ Management UI |
| コンテキスト読み書き（Redis） | < 10ms | Redis INFO stats |
| タスク全体完了時間（標準） | < 30秒 | トレースID追跡 |

### 可観測性

```mermaid
graph LR
    subgraph AGENTS["エージェント群"]
        OA[OA] --> SA[SA]
        OA --> CA[CA]
    end

    subgraph OBS["可観測性スタック"]
        direction TB
        TR[🔭 Jaeger<br/>分散トレーシング]
        MT[📈 Prometheus<br/>メトリクス収集]
        LG[📋 OpenSearch<br/>ログ集約]
        AL[🚨 Alertmanager<br/>アラート]
    end

    AGENTS -->|OpenTelemetry SDK| TR
    AGENTS -->|Prometheus Exporter| MT
    AGENTS -->|Fluent Bit| LG
    MT --> AL

    style OBS fill:#f3e5f5,stroke:#6a1b9a
```

#### 必須の計装ポイント

- **分散トレース**: 全エージェント呼び出しに `trace_id` / `span_id` を付与し、Jaegerで可視化
- **メトリクス**: タスク成功率・失敗率・レイテンシをPrometheusで収集
- **ログ**: 構造化ログ（JSON）で出力し、`correlation_id` で横断検索可能にする

### セキュリティ

| 脅威 | 対策 |
|---|---|
| プロンプトインジェクション | ユーザー入力のサニタイズ + Guardrailsによる出力検証 |
| エージェント間なりすまし | 内部認証トークン（JWT）による送信元検証 |
| 機密情報の漏洩 | PII検出フィルター（Microsoft Presidio）をパイプラインに組み込み |
| 不正ツール呼び出し | ツール実行権限をエージェントごとにRBACで制限 |

---

> **📝 レビュー記録** ([1.4.14 レビュー](./1.4.14_レビュー.md) 参照)
>
> **📝 修正記録** ([1.4.15 レビュー後修正](./1.4.15_レビュー後修正.md) 参照)
